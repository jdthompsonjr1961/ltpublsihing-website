# LT Publishing redesign — Phase 2

Upload the contents of this folder to the root of the `site-redesign` branch, preserving the folders.

This phase:
- makes Custom Status Label optional;
- supplies automatic status wording in public pages;
- replaces the oversized Decap preview with compact cover cards;
- overwrites root `admin.html` with a redirect to the real Decap manager;
- makes author and imprint pages catalogue-driven;
- modernizes Authors, Services, and Belle Époque Editions pages;
- leaves `data/books.json` and the `/assets/` catalogue data untouched.

After upload, test `/admin/`, edit one book, and confirm the cover appears in the compact preview. Do not merge into `main` yet.
